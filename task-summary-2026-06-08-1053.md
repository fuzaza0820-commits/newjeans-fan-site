# PixelCraft 项目框架

## 目标
在线像素化图片处理工具，用户上传图片后实时预览像素化效果并导出高清图。

## 方案确认
- **技术栈：纯前端（方案 A）** — React 18 + TypeScript + Tailwind CSS + Vite
- **预设风格：已内置 4 个预设**（复古像素 / 马赛克 / 赛博朋克 / 低多边形）
- **画笔局部处理：暂不纳入**（可后续扩展）

## 项目结构
```
pixelcraft/
├── src/
│   ├── utils/
│   │   └── pixelate.ts       ← 像素化核心算法
│   ├── components/
│   │   ├── ImageUploader.tsx  ← 拖拽上传组件
│   │   ├── PixelCanvas.tsx   ← Canvas 渲染 + 实时预览
│   │   └── ControlPanel.tsx  ← 参数控制面板
│   ├── App.tsx               ← 主布局
│   └── index.css             ← Tailwind + 自定义样式
├── index.html
├── tailwind.config.js
└── dist/                     ← 构建产物
```

## 核心参数
| 参数 | 范围 | 说明 |
|---|---|---|
| 像素大小 | 1~50 px | 像素网格边长 |
| 清晰度 | 0~100% | 模糊融合 ↔ 边缘锐化 |
| 过渡效果 | 5 档 | hard / slight / medium / smooth / gaussian |
| 输出格式 | PNG / JPG | PNG 保留透明通道 |
| 输出质量 | 10~100% | JPG 压缩质量 |

## 构建状态
- ✅ TypeScript 编译通过
- ✅ Vite 生产构建通过
- ✅ Tailwind CSS 集成完成

## 启动方式
```bash
cd pixelcraft
npm run dev      # 开发模式（localhost:5173）
npm run build    # 生产构建
```
