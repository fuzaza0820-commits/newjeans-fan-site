# PixelCraft 项目进度

## 目标
在线像素化图片处理工具，纯前端，图片不上传服务器。

## 当前完成功能

### P0（已上线）
- 图片上传（拖拽/点击，PNG/JPG/WebP）
- 像素大小 1~50px 实时预览
- 清晰度 0~100% 滑块
- 5 档过渡效果（硬边→高斯模糊）
- PNG / JPG 输出 + 质量调节
- 4 个预设风格一键切换

### P1 — 画笔局部像素化（✅ 本次完成）
- 画笔模式：白色涂抹=像素化，橡皮=恢复原图
- 可调节笔刷大小（5~80px）
- 蒙版调试视图（显示当前画笔覆盖区域）
- 清空蒙版按钮

### P1 — 历史记录/撤销重做（✅ 本次完成）
- Ctrl+Z 撤销，Ctrl+Shift+Z 重做
- 最多保留 20 步
- 右侧控制面板显示撤销/重做按钮（可点击）
- 基于 ImageData 快照，精确还原每一帧

### P2 — 高清放大导出（✅ 本次完成）
- 支持 1× / 2× / 3× / 4× 放大导出
- 超出原图分辨率，适合印刷/大屏用途
- 下载文件名含分辨率标识

## 项目结构
```
pixelcraft/
├── src/
│   ├── utils/
│   │   ├── pixelate.ts        ← v2：全局+蒙版+高清放大
│   │   └── HistoryManager.ts  ← 撤销/重做栈管理
│   ├── components/
│   │   ├── ImageUploader.tsx
│   │   ├── PixelCanvas.tsx    ← 画笔蒙版 + 历史快照 + 放大导出
│   │   └── ControlPanel.tsx   ← 导出倍率 + 历史按钮
│   ├── App.tsx
│   └── index.css
└── dist/ (✅ 构建成功)
```

## 构建状态
✅ TypeScript clean
✅ Vite build clean

## 待做（P2）
- 批量处理（多图队列）
- 分享链接（参数编码 URL）
- 移动端适配
- 预设自定义保存
